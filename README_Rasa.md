## Generate PDDL 

 Navigate to ./source 
 
 Run `python3 rasa_pddl.py`

 Example output

 ```
 [?a - account(user(object)), ?u - user(object)]
        (:action None
                :parameters ()
                :precondition (and

                )
                :effect (and

                )
        )


 ``` 